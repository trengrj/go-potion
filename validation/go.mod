// This stub makes validation/ a nested module so its contents (reference
// samples and the generator script) are excluded from the published module
// zip; it is never tagged or built.
module github.com/trengrj/go-potion/validation

go 1.25.0
